// Declara uma tupla chamada 'point' que deve conter um número e uma string
let point:[number, string];

// Atribui valores à tupla 'point'
point = [23, 'kjgb'];

// Desestrutura a tupla 'point' em duas variáveis: 'x' e 'y'
let [x, y] = point;

// Declara um array de tuplas chamado 'coisas', onde cada tupla contém uma string e um número
let coisas:[string, number][];

// Atribui valores ao array de tuplas 'coisas'
coisas = [
    ['corda', 10],
    ['mangueira', 20],
    ['vasoura', 2]
];
